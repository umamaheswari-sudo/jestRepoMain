# API Test Suite

Generated API tests using Jest and Axios.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```

2. Copy environment file and configure:
   ```bash
   cp .env.example .env
   ```

3. Edit `.env` with your API configuration:
   - `BASE_URL`: Your API base URL
   - `API_KEY`: Your API key (if required)
   - `AUTH_TOKEN`: Your authentication token (if required)

## Running Tests

Run all tests:
```bash
npm test
```

Run tests in watch mode:
```bash
npm run test:watch
```

Run tests with coverage:
```bash
npm run test:coverage
```

## Project Structure

```
javascript_tests/
├── tests/
│   └── api.test.js          # Generated test cases
├── utils/
│   └── apiClient.js         # Axios wrapper with interceptors
├── config/
│   └── settings.js          # Environment configuration
├── package.json             # Dependencies and scripts
├── jest.config.js           # Jest configuration
├── .env.example             # Environment variables template
└── README.md                # This file
```

## Generated by SFIT
